import { useLoaderData } from "react-router-dom";

const Givemark = () => {
    const giveMark = useLoaderData();
    return (
        <div>
         hello world
        </div>
    );
};

export default Givemark;